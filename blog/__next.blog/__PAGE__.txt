1:"$Sreact.fragment"
2:I[22016,["/_next/static/chunks/3rco3j59jf45b.js","/_next/static/chunks/14mrh2-p_w84d.js","/_next/static/chunks/2snm_u37n2zlf.js"],""]
3:I[29436,["/_next/static/chunks/3rco3j59jf45b.js","/_next/static/chunks/14mrh2-p_w84d.js","/_next/static/chunks/2snm_u37n2zlf.js"],"default"]
b:I[97367,["/_next/static/chunks/3rco3j59jf45b.js","/_next/static/chunks/14mrh2-p_w84d.js"],"OutletBoundary"]
c:"$Sreact.suspense"
0:{"rsc":["$","$1","c",{"children":[["$","main",null,{"className":"relative z-10 px-6 pb-20 pt-28","children":["$","div",null,{"className":"mx-auto max-w-6xl","children":[["$","$L2",null,{"href":"/#blog","className":"mb-8 inline-flex items-center gap-2 font-mono text-xs text-slate-500 transition-colors hover:text-emerald-400","children":[["$","svg",null,{"className":"h-3.5 w-3.5","fill":"none","stroke":"currentColor","viewBox":"0 0 24 24","aria-hidden":"true","children":["$","path",null,{"strokeLinecap":"round","strokeLinejoin":"round","strokeWidth":2,"d":"M15 19l-7-7 7-7"}]}],"Back to home"]}],["$","div",null,{"className":"mb-12","children":[["$","p",null,{"className":"mb-2 font-mono text-xs uppercase tracking-[0.2em] text-emerald-400/70","children":"Writing & notes"}],["$","h1",null,{"className":"text-3xl font-bold tracking-tight text-white sm:text-4xl","children":"Blog"}],["$","p",null,{"className":"mt-3 max-w-2xl text-slate-400","children":["All posts — ",5," article","s"]}],["$","div",null,{"className":"section-glow mt-6 max-w-xs"}]]}],["$","div",null,{"className":"grid gap-6 sm:grid-cols-2 lg:grid-cols-3","children":[["$","$L3","deploying-nodejs-and-nextjs-applications-with-pm2-and-nginx",{"post":{"slug":"deploying-nodejs-and-nextjs-applications-with-pm2-and-nginx","title":"Deploying Node.js and Next.js Applications with PM2 and Nginx","excerpt":"Learn how to deploy Node.js and Next.js applications on a Linux server using PM2 and Nginx, configure reverse proxying, enable automatic process restarts, and run production applications reliably.","date":"2026-07-20","readTime":"10 min read","tags":["Node.js","Next.js","PM2","Nginx","Deployment"],"coverImage":"/blogs/deploy/1.webp","content":[{"type":"paragraph","text":"Deploying a Node.js or Next.js application to a production server involves more than simply starting the application with npm run start. A production application needs a process manager to keep the application running, automatically restart it when necessary, and a web server to handle incoming requests efficiently. PM2 and Nginx are two popular tools that work well together for this purpose."},{"type":"paragraph","text":"In this guide, we will deploy a Node.js or Next.js application on a Linux server using PM2 as the process manager and Nginx as a reverse proxy. We will also configure the application to run in production, connect it to a domain, and make sure it can restart automatically after a server reboot."},{"type":"heading","text":"What is PM2?"},{"type":"paragraph","text":"PM2 is a production process manager for Node.js applications. Instead of running your application directly with node or npm, PM2 manages the application process and keeps it running in the background."},{"type":"paragraph","text":"PM2 can automatically restart an application when it crashes, monitor application processes, manage logs, run applications in cluster mode, and restore processes after a server restart. This makes it useful for running Node.js and Next.js applications in production."},{"type":"heading","text":"What is Nginx?"},{"type":"paragraph","text":"Nginx is a high performance web server and reverse proxy. In a typical Node.js deployment, the application runs on an internal port such as 3000, while Nginx listens for public HTTP and HTTPS requests."},{"type":"paragraph","text":"When a user visits your domain, Nginx receives the request and forwards it to the Node.js or Next.js application running on the local server. This setup keeps the application server separate from the public facing web server and makes it easier to configure domains, SSL certificates, redirects, and other web server features."},{"type":"heading","text":"The deployment architecture"},{"type":"paragraph","text":"The basic architecture is straightforward. The user sends a request to your domain, Nginx receives the request, and Nginx forwards it to the application managed by PM2. The application then processes the request and sends the response back through Nginx."},{"type":"code","language":"text","filename":"Architecture","code":"User\n  ↓\nNginx (Port 80 / 443)\n  ↓\nPM2\n  ↓\nNode.js / Next.js Application (Port 3000)"},{"type":"paragraph","text":"This architecture allows the Node.js application to run on a local port while Nginx handles public traffic. You do not need to expose the application port directly to the internet."},{"type":"heading","text":"Prepare the server"},{"type":"paragraph","text":"Before deploying the application, you need a Linux server with Node.js, npm, Git, and Nginx installed. Ubuntu is a common choice for hosting Node.js applications."},{"type":"paragraph","text":"First, update the server packages."},{"type":"code","language":"bash","filename":"Terminal","code":"sudo apt update && sudo apt upgrade -y"},{"type":"paragraph","text":"You can then install Node.js and npm using your preferred installation method. After installation, verify that Node.js and npm are available."},{"type":"code","language":"bash","filename":"Terminal","code":"node -v\nnpm -v"},{"type":"heading","text":"Install PM2"},{"type":"paragraph","text":"PM2 can be installed globally using npm. Installing it globally allows you to use the pm2 command from anywhere on the server."},{"type":"code","language":"bash","filename":"Terminal","code":"sudo npm install -g pm2"},{"type":"paragraph","text":"After installation, verify that PM2 is available."},{"type":"code","language":"bash","filename":"Terminal","code":"pm2 -v"},{"type":"heading","text":"Upload or clone your application"},{"type":"paragraph","text":"Once the server is ready, place your application inside a directory on the server. If your project is stored in Git, you can clone the repository directly."},{"type":"code","language":"bash","filename":"Terminal","code":"cd /var/www\nsudo git clone YOUR_REPOSITORY_URL my-app\ncd my-app"},{"type":"paragraph","text":"You can also upload the project files using tools such as SFTP or an FTP client, depending on your hosting environment. The important part is to make sure the application is located in a directory where your deployment user has the required permissions."},{"type":"heading","text":"Install project dependencies"},{"type":"paragraph","text":"After moving into the project directory, install the application's dependencies."},{"type":"code","language":"bash","filename":"Terminal","code":"npm install"},{"type":"paragraph","text":"For production deployments, npm ci can also be used when the project contains a package-lock.json file. This installs the exact dependency versions defined in the lock file."},{"type":"code","language":"bash","filename":"Terminal","code":"npm ci"},{"type":"heading","text":"Build a Next.js application"},{"type":"paragraph","text":"Next.js applications should be built before starting them in production mode. The build process creates the optimized production output required by Next.js."},{"type":"code","language":"bash","filename":"Terminal","code":"npm run build"},{"type":"paragraph","text":"After the build completes successfully, the application can be started using the production start command."},{"type":"code","language":"bash","filename":"Terminal","code":"npm run start"},{"type":"paragraph","text":"By default, a Next.js application usually listens on port 3000. You can test the application directly from the server using curl."},{"type":"code","language":"bash","filename":"Terminal","code":"curl http://localhost:3000"},{"type":"heading","text":"Run the application with PM2"},{"type":"paragraph","text":"Instead of running npm run start directly, PM2 can manage the production application. For a Next.js application, PM2 can execute the npm start command."},{"type":"code","language":"bash","filename":"Terminal","code":"pm2 start npm --name 'my-next-app' -- start"},{"type":"paragraph","text":"The --name option gives the process a readable name. This makes it easier to identify and manage the application when multiple Node.js applications are running on the same server."},{"type":"paragraph","text":"You can check all running PM2 processes with the following command."},{"type":"code","language":"bash","filename":"Terminal","code":"pm2 list"},{"type":"heading","text":"Manage the application with PM2"},{"type":"paragraph","text":"PM2 provides simple commands for managing your application. You can restart the application after deploying new code, stop it when necessary, and remove it from the PM2 process list."},{"type":"code","language":"bash","filename":"Terminal","code":"pm2 restart my-next-app\npm2 stop my-next-app\npm2 delete my-next-app"},{"type":"paragraph","text":"You can also view the application's output and error logs using the PM2 logs command."},{"type":"code","language":"bash","filename":"Terminal","code":"pm2 logs my-next-app"},{"type":"heading","text":"Configure PM2 to start after reboot"},{"type":"paragraph","text":"One of the main advantages of PM2 is that it can restore your applications after a server restart. First, generate the startup command for your system."},{"type":"code","language":"bash","filename":"Terminal","code":"pm2 startup"},{"type":"paragraph","text":"PM2 will display a command that should be executed with the required system permissions. Run that command and then save the currently running PM2 processes."},{"type":"code","language":"bash","filename":"Terminal","code":"pm2 save"},{"type":"paragraph","text":"After this configuration, PM2 can automatically restore the saved processes when the server starts again."},{"type":"heading","text":"Install Nginx"},{"type":"paragraph","text":"Next, install Nginx on the server. Nginx will act as the reverse proxy between the public internet and your Node.js or Next.js application."},{"type":"code","language":"bash","filename":"Terminal","code":"sudo apt install nginx -y"},{"type":"paragraph","text":"After installation, check whether Nginx is running."},{"type":"code","language":"bash","filename":"Terminal","code":"sudo systemctl status nginx"},{"type":"paragraph","text":"If Nginx is not running, you can start it using systemctl."},{"type":"code","language":"bash","filename":"Terminal","code":"sudo systemctl start nginx"},{"type":"heading","text":"Configure Nginx as a reverse proxy"},{"type":"paragraph","text":"Create a new Nginx server configuration for your domain. On Ubuntu, site-specific configurations are commonly stored inside /etc/nginx/sites-available."},{"type":"code","language":"bash","filename":"Terminal","code":"sudo nano /etc/nginx/sites-available/my-next-app"},{"type":"paragraph","text":"Add a server block that forwards requests to the Next.js application running on port 3000."},{"type":"code","language":"nginx","filename":"my-next-app","code":"server {\n    listen 80;\n    server_name example.com [www.example.com;\n\n](http://www.example.com;\n\n)    location / {\n        proxy_pass [http://127.0.0.1:3000;\n](http://127.0.0.1:3000;\n)        proxy_http_version 1.1;\n\n        proxy_set_header Upgrade $http_upgrade;\n        proxy_set_header Connection 'upgrade';\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto $scheme;\n    }\n}"},{"type":"paragraph","text":"The proxy_pass directive forwards requests to the application running on port 3000. The proxy headers preserve useful information about the original client request and are commonly used when running applications behind a reverse proxy."},{"type":"heading","text":"Enable the Nginx site"},{"type":"paragraph","text":"After creating the configuration, create a symbolic link inside the sites-enabled directory."},{"type":"code","language":"bash","filename":"Terminal","code":"sudo ln -s /etc/nginx/sites-available/my-next-app /etc/nginx/sites-enabled/"},{"type":"paragraph","text":"Before restarting Nginx, always test the configuration for syntax errors."},{"type":"code","language":"bash","filename":"Terminal","code":"sudo nginx -t"},{"type":"paragraph","text":"If the configuration test is successful, reload Nginx to apply the changes."},{"type":"code","language":"bash","filename":"Terminal","code":"sudo systemctl reload nginx"},{"type":"heading","text":"Point your domain to the server"},{"type":"paragraph","text":"Your domain must point to the public IP address of the server. Create an A record for the domain and point it to your server's IP address."},{"type":"code","language":"text","filename":"DNS","code":"Type: A\nName: @\nValue: YOUR_SERVER_IP\n\nType: A\nName: www\nValue: YOUR_SERVER_IP"},{"type":"paragraph","text":"DNS changes may take some time to propagate. Once the domain resolves to the server, Nginx should receive the request and forward it to the application managed by PM2."},{"type":"heading","text":"Enable HTTPS with SSL"},{"type":"paragraph","text":"Running a production application over HTTPS is strongly recommended. One common approach on Ubuntu is to use Certbot with Nginx to obtain and configure a Let's Encrypt SSL certificate."},{"type":"code","language":"bash","filename":"Terminal","code":"sudo apt install certbot python3-certbot-nginx -y"},{"type":"paragraph","text":"After installing Certbot, request a certificate for your domain."},{"type":"code","language":"bash","filename":"Terminal","code":"sudo certbot --nginx -d example.com -d [www.example.com](http://www.example.com)"},{"type":"paragraph","text":"Certbot can update the Nginx configuration and configure HTTPS for the domain. It can also set up automatic certificate renewal so that the certificate does not expire without being renewed."},{"type":"heading","text":"Use environment variables in production"},{"type":"paragraph","text":"Production applications commonly require environment variables for API URLs, database connections, authentication secrets, and other configuration values. These values should not be hardcoded directly into the application source code."},{"type":"paragraph","text":"For a Next.js application, create the appropriate environment file on the server and make sure the required variables are available before running the production build."},{"type":"code","language":"bash","filename":".env.production","code":"NEXT_PUBLIC_API_URL=https://api.example.com\nDATABASE_URL=your_database_connection\nAUTH_SECRET=your_secret"},{"type":"paragraph","text":"If an environment variable is required during the Next.js build process, make sure it is available before running npm run build. After changing environment variables that are included in the build output, you may need to rebuild the application."},{"type":"heading","text":"Deploy updates to the application"},{"type":"paragraph","text":"When new code is pushed to the repository, the deployment process typically involves pulling the latest changes, installing any new dependencies, rebuilding the application, and restarting the PM2 process."},{"type":"code","language":"bash","filename":"Terminal","code":"cd /var/www/my-app\n\ngit pull origin main\n\nnpm ci\n\nnpm run build\n\npm2 restart my-next-app"},{"type":"paragraph","text":"For a simple deployment, these commands are often enough to update a running Next.js application. More advanced projects can automate this workflow using CI/CD pipelines so that deployments happen automatically after changes are merged."},{"type":"heading","text":"Node.js applications use the same architecture"},{"type":"paragraph","text":"The same PM2 and Nginx architecture can be used for a regular Node.js application. The main difference is the command used to start the application."},{"type":"code","language":"bash","filename":"Terminal","code":"pm2 start server.js --name 'my-node-app'"},{"type":"paragraph","text":"If the Node.js application uses an npm start script, you can also start it through npm using PM2."},{"type":"code","language":"bash","filename":"Terminal","code":"pm2 start npm --name 'my-node-app' -- start"},{"type":"paragraph","text":"Nginx can then forward requests to the port where the Node.js server is listening, just as it does with a Next.js application."},{"type":"heading","text":"Check logs when something goes wrong"},{"type":"paragraph","text":"When a production application is not working as expected, checking logs should be one of the first troubleshooting steps. PM2 provides access to application output and error logs."},{"type":"code","language":"bash","filename":"Terminal","code":"pm2 logs my-next-app"},{"type":"paragraph","text":"You can also inspect the Nginx error log when requests are not reaching the application correctly."},{"type":"code","language":"bash","filename":"Terminal","code":"sudo tail -f /var/log/nginx/error.log"},{"type":"paragraph","text":"If the application works at http://localhost:3000 but does not work through the domain, the problem is often related to Nginx configuration, DNS, firewall rules, or SSL configuration."},{"type":"heading","text":"Common deployment checklist"},{"type":"paragraph","text":"Before considering the deployment complete, it is useful to verify each part of the stack."},{"type":"code","language":"text","filename":"Deployment Checklist","code":"✓ Node.js is installed\n✓ Application dependencies are installed\n✓ Production build completes successfully\n✓ Application runs correctly on localhost\n✓ PM2 process is running\n✓ PM2 startup configuration is enabled\n✓ PM2 processes have been saved\n✓ Nginx is running\n✓ Nginx configuration passes nginx -t\n✓ Domain DNS points to the server\n✓ Nginx reverse proxy is configured\n✓ HTTPS is enabled\n✓ Production environment variables are configured"},{"type":"heading","text":"Final thoughts"},{"type":"paragraph","text":"Deploying a Node.js or Next.js application with PM2 and Nginx provides a practical foundation for running production applications on a Linux server. PM2 keeps the application process running and helps manage restarts and logs, while Nginx handles incoming web traffic and acts as a reverse proxy."},{"type":"paragraph","text":"The basic workflow is straightforward: prepare the server, install dependencies, build the application, start it with PM2, configure Nginx as a reverse proxy, point the domain to the server, and enable HTTPS. Once this setup is in place, you have a reliable foundation that can be extended with automated deployments, monitoring, load balancing, and other production infrastructure."},{"type":"paragraph","text":"For smaller projects, this setup is often enough to run a production application reliably. As the application grows, you can build on the same foundation by introducing CI/CD pipelines, containerization, centralized logging, monitoring, and scalable infrastructure."}]},"saveScroll":false}],"$L4","$L5","$L6","$L7"]}]]}]}],["$L8"],"$L9"]}],"isPartial":false,"staleTime":300,"varyParams":null,"buildId":"gfqUbKtt9YKYVeMYMN3g4"}
4:["$","$L3","building-scalable-rest-apis-with-nodejs-and-express",{"post":{"slug":"building-scalable-rest-apis-with-nodejs-and-express","title":"Building Scalable REST APIs with Node.js and Express","excerpt":"A practical guide to structuring Node.js and Express APIs with clean architecture, validation, authentication, and consistent error handling.","date":"2026-06-15","readTime":"8 min read","tags":["Node.js","Express","REST API","Backend"],"coverImage":"/blogs/nodejs-rest-api/1.webp","content":[{"type":"paragraph","text":"Building a Node.js backend that works for a small project is relatively easy. The challenge starts when the application grows and the codebase needs to support more users, more features, and more developers. Without a clear structure, controllers become too large, business logic gets duplicated, and debugging becomes increasingly difficult."},{"type":"paragraph","text":"When building REST APIs with Node.js and Express, I prefer separating responsibilities between routes, controllers, services, and data-access layers. This keeps HTTP concerns separate from business logic and makes individual parts of the application easier to test and maintain."},{"type":"heading","text":"Start with a predictable project structure"},{"type":"paragraph","text":"A good folder structure does not automatically make an application scalable, but a predictable structure makes it much easier to understand where new functionality belongs. For example, routes can handle endpoint definitions, controllers can handle request and response logic, and services can contain reusable business rules."},{"type":"code","language":"text","filename":"project-structure","code":"src/\n├── controllers/\n├── services/\n├── routes/\n├── models/\n├── middlewares/\n├── validators/\n├── utils/\n├── config/\n└── app.js"},{"type":"heading","text":"Keep controllers thin"},{"type":"paragraph","text":"Controllers should primarily translate HTTP requests into application actions and return appropriate responses. Complex business logic is better placed in service functions so it can be reused by different endpoints or background jobs."},{"type":"code","language":"javascript","filename":"lead.controller.js","code":"import leadService from \"../services/lead.service.js\";\n\nexport async function createLead(req, res, next) {\n  try {\n    const lead = await leadService.createLead({\n      ...req.body,\n      createdBy: req.user.id,\n    });\n\n    return res.status(201).json({\n      success: true,\n      data: lead,\n    });\n  } catch (error) {\n    next(error);\n  }\n}"},{"type":"heading","text":"Validate input before it reaches business logic"},{"type":"paragraph","text":"API endpoints should never assume that incoming data is valid. Request validation helps prevent malformed data from reaching the database and makes API behavior more predictable. Libraries such as Zod, Joi, or express-validator can be used depending on the project."},{"type":"code","language":"javascript","filename":"lead.validator.js","code":"import { z } from \"zod\";\n\nexport const createLeadSchema = z.object({\n  name: z.string().min(2),\n  email: z.string().email(),\n  phone: z.string().min(10),\n});"},{"type":"heading","text":"Use centralized error handling"},{"type":"paragraph","text":"Instead of handling every error differently inside each controller, a centralized error middleware can provide a consistent response format. This also makes it easier to hide internal errors in production while logging the details on the server."},{"type":"code","language":"javascript","filename":"error.middleware.js","code":"export function errorHandler(error, req, res, next) {\n  console.error(error);\n\n  return res.status(error.statusCode || 500).json({\n    success: false,\n    message: error.message || \"Internal server error\",\n  });\n}"},{"type":"image","src":"/blogs/nodejs-rest-api/2.webp","alt":"Node.js REST API"},{"type":"heading","text":"Authentication is only one part of security"},{"type":"paragraph","text":"JWT-based authentication is commonly used in Node.js applications, but verifying a token is only the beginning. APIs should also check authorization, validate resource ownership, protect sensitive routes, rate limit public endpoints, and avoid exposing internal implementation details in error responses."},{"type":"paragraph","text":"A scalable backend is not defined by how many lines of code it contains. It is defined by how easily the team can add features without introducing unnecessary complexity. Clear separation of concerns, validation, centralized errors, and consistent API contracts provide a strong foundation for growing Node.js applications."}]},"saveScroll":false}]
a:T497,"use client";

import { useRef } from "react";
import { useVirtualizer } from "@tanstack/react-virtual";

export default function DataTable({ data }) {
  const parentRef = useRef(null);

  const rowVirtualizer = useVirtualizer({
    count: data.length,
    getScrollElement: () => parentRef.current,
    estimateSize: () => 50,
    overscan: 5,
  });

  return (
    <div
      ref={parentRef}
      style={{
        height: "600px",
        overflow: "auto",
      }}
    >
      <div
        style={{
          height: `${rowVirtualizer.getTotalSize()}px`,
          position: "relative",
        }}
      >
        {rowVirtualizer.getVirtualItems().map((virtualRow) => {
          const row = data[virtualRow.index];

          return (
            <div
              key={virtualRow.key}
              style={{
                position: "absolute",
                top: 0,
                left: 0,
                width: "100%",
                height: `${virtualRow.size}px`,
                transform: `translateY(${virtualRow.start}px)`,
              }}
            >
              {row.name}
            </div>
          );
        })}
      </div>
    </div>
  );
}5:["$","$L3","virtualizing-large-tables-with-tanstack-virtual",{"post":{"slug":"virtualizing-large-tables-with-tanstack-virtual","title":"Virtualizing Large Tables with TanStack Virtual","excerpt":"Learn how to improve the performance of large React tables by rendering only visible rows with TanStack Virtual, reducing DOM size, improving scrolling, and creating scalable data-heavy interfaces.","date":"2026-05-02","readTime":"8 min read","tags":["React","TanStack Virtual","Performance"],"coverImage":"/blogs/table-virtualization/1.webp","content":[{"type":"paragraph","text":"Tables are one of the most common places where frontend performance problems become noticeable. Rendering a few dozen rows is usually not an issue, but displaying thousands of rows at the same time can create a large DOM tree, increase memory usage, and make scrolling feel slow. Instead of rendering every row, virtualization allows us to render only the rows currently visible inside the viewport."},{"type":"heading","text":"What is table virtualization?"},{"type":"paragraph","text":"Table virtualization is a technique where only the visible rows of a large dataset are rendered to the DOM. As the user scrolls, the virtualized list removes rows that are no longer visible and renders the rows that have entered the viewport. The table can still represent thousands of records, but the browser only has to manage a small number of actual DOM elements at any moment."},{"type":"paragraph","text":"This is especially useful for admin dashboards, analytics applications, CRM systems, reporting tools, and other interfaces where users need to work with large datasets."},{"type":"heading","text":"TanStack Table and TanStack Virtual solve different problems"},{"type":"paragraph","text":"When building a large React table, it is useful to understand the difference between table management and virtualization. TanStack Table handles table functionality such as columns, sorting, filtering, pagination, and row models. TanStack Virtual handles efficient rendering by calculating which items should actually be displayed."},{"type":"paragraph","text":"Using them together provides a flexible solution for building high-performance data tables. The table library manages the data and behavior, while the virtualization library controls how many rows are mounted in the DOM."},{"type":"heading","text":"Install TanStack Virtual"},{"type":"code","language":"bash","filename":"Terminal","code":"npm install @tanstack/react-virtual"},{"type":"paragraph","text":"The React Virtual package provides hooks that can calculate the visible range of items inside a scrollable container. We can then use that information to render only the rows that are currently needed."},{"type":"heading","text":"Create a virtualized table"},{"type":"paragraph","text":"A basic virtualized table starts with a scrollable container. The virtualizer needs a reference to this element so it can monitor the scroll position and determine which rows should be rendered."},{"type":"code","language":"javascript","filename":"DataTable.jsx","code":"$a"},{"type":"paragraph","text":"The important part of this implementation is that data.length can represent thousands of records, while getVirtualItems returns only the rows that need to be displayed. The inner container keeps the correct total height, which makes the scrollbar represent the full dataset even though most rows are not mounted."},{"type":"heading","text":"Use overscan for smoother scrolling"},{"type":"paragraph","text":"The overscan option controls how many additional rows are rendered outside the visible area. Rendering a few extra rows helps prevent blank spaces from appearing when the user scrolls quickly because the next rows are already mounted before they enter the viewport."},{"type":"code","language":"javascript","filename":"virtualizer.js","code":"const rowVirtualizer = useVirtualizer({\n  count: data.length,\n  getScrollElement: () => parentRef.current,\n  estimateSize: () => 50,\n  overscan: 10,\n});"},{"type":"paragraph","text":"There is a trade-off when choosing the overscan value. A larger value can make fast scrolling smoother, but it also increases the number of DOM elements being rendered. For most tables, a small value such as 5 to 10 is a reasonable starting point."},{"type":"heading","text":"Combine TanStack Table with TanStack Virtual"},{"type":"paragraph","text":"For a production data table, I usually recommend combining TanStack Table with TanStack Virtual. TanStack Table can manage sorting, filtering, and row models, while TanStack Virtual can efficiently render the resulting rows."},{"type":"code","language":"javascript","filename":"DataTable.jsx","code":"const table = useReactTable({\n  data,\n  columns,\n  getCoreRowModel: getCoreRowModel(),\n  getSortedRowModel: getSortedRowModel(),\n});\n\nconst rows = table.getRowModel().rows;\n\nconst rowVirtualizer = useVirtualizer({\n  count: rows.length,\n  getScrollElement: () => parentRef.current,\n  estimateSize: () => 50,\n  overscan: 5,\n});"},{"type":"paragraph","text":"This approach keeps the table logic separate from the rendering strategy. Sorting and filtering can change the rows returned by TanStack Table, and the virtualizer automatically works with the updated row collection."},{"type":"heading","text":"Be careful with dynamic row heights"},{"type":"paragraph","text":"Virtualization works best when row heights are predictable. If every row has the same height, estimateSize is straightforward and the virtualizer can calculate positions efficiently. However, tables with wrapped text, expandable content, or dynamically sized rows require more careful measurement."},{"type":"code","language":"javascript","filename":"virtualizer.js","code":"const rowVirtualizer = useVirtualizer({\n  count: rows.length,\n  getScrollElement: () => parentRef.current,\n  estimateSize: () => 50,\n  measureElement: (element) => element?.getBoundingClientRect().height,\n  overscan: 5,\n});"},{"type":"paragraph","text":"When row sizes can change, measuring the rendered elements helps the virtualizer maintain more accurate positions. In practice, keeping table rows consistent whenever possible usually results in a simpler and more predictable implementation."},{"type":"heading","text":"Virtualization is not a replacement for pagination"},{"type":"paragraph","text":"Virtualization and pagination solve different problems. Virtualization improves the rendering performance of data that is already available in the browser. Pagination limits how much data is loaded and processed in the first place."},{"type":"paragraph","text":"If an API returns 100,000 records, virtualizing the table can reduce the DOM workload, but the browser still has to download and store all 100,000 records. For very large datasets, server-side pagination or infinite scrolling combined with virtualization is often a better architecture."},{"type":"heading","text":"A practical architecture for large datasets"},{"type":"paragraph","text":"For a large production application, I would typically combine server-side filtering, sorting, and pagination with client-side virtualization. The server returns only the relevant records, while TanStack Virtual ensures that the current page or loaded dataset does not create an unnecessarily large DOM tree."},{"type":"code","language":"javascript","filename":"DataTable.jsx","code":"const virtualRows = rowVirtualizer.getVirtualItems();\n\nreturn (\n  <div ref={parentRef} className=\"h-[600px] overflow-auto\">\n    <div\n      style={{\n        height: `${rowVirtualizer.getTotalSize()}px`,\n        position: \"relative\",\n      }}\n    >\n      {virtualRows.map((virtualRow) => {\n        const row = rows[virtualRow.index];\n\n        return (\n          <div\n            key={row.id}\n            ref={rowVirtualizer.measureElement}\n            data-index={virtualRow.index}\n            className=\"absolute left-0 top-0 w-full\"\n            style={{\n              transform: `translateY(${virtualRow.start}px)`,\n            }}\n          >\n            {row.getVisibleCells().map((cell) => (\n              <div key={cell.id}>\n                {flexRender(\n                  cell.column.columnDef.cell,\n                  cell.getContext()\n                )}\n              </div>\n            ))}\n          </div>\n        );\n      })}\n    </div>\n  </div>\n);"},{"type":"heading","text":"Measure performance before and after"},{"type":"paragraph","text":"The biggest benefit of virtualization becomes noticeable when a table contains hundreds or thousands of rows. However, it is still important to measure the actual performance of your application. Browser DevTools can help identify long rendering tasks, excessive DOM nodes, memory usage, and slow event handlers."},{"type":"paragraph","text":"Virtualization is especially valuable when users experience slow initial rendering, laggy scrolling, or high memory consumption caused by large numbers of DOM elements. If the dataset is small, adding virtualization may introduce unnecessary complexity without providing a meaningful benefit."},{"type":"heading","text":"Final thoughts"},{"type":"paragraph","text":"Large tables do not have to become a performance bottleneck. TanStack Virtual provides a practical way to render only the rows users can actually see, while maintaining the scrolling experience of a much larger dataset. When combined with TanStack Table, server-side pagination, and efficient data fetching, it becomes possible to build responsive data-heavy interfaces that can scale as the application grows."},{"type":"paragraph","text":"The key is to choose the right optimization for the problem. Use virtualization when the DOM becomes too large, use pagination when the dataset itself is too large to load at once, and combine both techniques when building applications that need to handle significant amounts of data."}]},"saveScroll":false}]
6:["$","$L3","building-secure-jwt-authentication-with-nodejs",{"post":{"slug":"building-secure-jwt-authentication-with-nodejs","title":"Building Secure JWT Authentication with Node.js","excerpt":"Understanding authentication in Node.js applications, from password hashing and JWTs to refresh tokens, authorization, and secure API design.","date":"2026-03-18","readTime":"10 min read","tags":["Node.js","Authentication","JWT","Security"],"coverImage":"/blogs/jwt-authentication/1.webp","content":[{"type":"paragraph","text":"Authentication is one of the first features added to most production applications, but it is also one of the easiest places to introduce security problems. A secure authentication system needs more than generating a JWT after login. Passwords must be stored safely, tokens must be handled carefully, and every protected operation must verify that the authenticated user is actually allowed to perform it."},{"type":"heading","text":"Never store plain-text passwords"},{"type":"paragraph","text":"Passwords should always be hashed before being stored in the database. A password hashing algorithm such as bcrypt or Argon2 makes it significantly harder for an attacker to recover the original passwords if the database is compromised."},{"type":"code","language":"javascript","filename":"password.js","code":"import bcrypt from \"bcrypt\";\n\nexport async function hashPassword(password) {\n  return bcrypt.hash(password, 12);\n}\n\nexport async function comparePassword(password, hash) {\n  return bcrypt.compare(password, hash);\n}"},{"type":"heading","text":"Create short-lived access tokens"},{"type":"paragraph","text":"Access tokens should generally have a limited lifetime. If an access token is compromised, a shorter expiration period reduces the amount of time it can be abused. Applications that need long-lived sessions can use refresh tokens to obtain new access tokens."},{"type":"code","language":"javascript","filename":"token.js","code":"import jwt from \"jsonwebtoken\";\n\nexport function createAccessToken(user) {\n  return jwt.sign(\n    {\n      userId: user._id.toString(),\n      role: user.role,\n    },\n    process.env.JWT_SECRET,\n    {\n      expiresIn: \"15m\",\n    }\n  );\n}"},{"type":"heading","text":"Authentication and authorization are different"},{"type":"paragraph","text":"Authentication answers the question: who is this user? Authorization answers a different question: what is this user allowed to do? A valid JWT does not automatically mean that the user can access every resource."},{"type":"code","language":"javascript","filename":"authorize.js","code":"export function authorize(...allowedRoles) {\n  return (req, res, next) => {\n    if (!allowedRoles.includes(req.user.role)) {\n      return res.status(403).json({\n        success: false,\n        message: \"You do not have permission to perform this action\",\n      });\n    }\n\n    next();\n  };\n}"},{"type":"heading","text":"Protect resources, not just routes"},{"type":"paragraph","text":"Role-based authorization is useful, but many applications also need resource-level authorization. For example, a user may have permission to update leads but should only be able to update leads belonging to their organization."},{"type":"code","language":"javascript","filename":"lead.controller.js","code":"const lead = await Lead.findOne({\n  _id: req.params.id,\n  organization: req.user.organization,\n});\n\nif (!lead) {\n  return res.status(404).json({\n    message: \"Lead not found\",\n  });\n}"},{"type":"heading","text":"Be careful with token storage"},{"type":"paragraph","text":"Where tokens are stored depends on the application's architecture and threat model. For browser applications, secure, HttpOnly cookies can reduce exposure to JavaScript-based token theft, while proper CSRF protections should also be considered. The important thing is to understand the trade-offs instead of treating token storage as a copy-and-paste decision."},{"type":"heading","text":"Add rate limiting and account protections"},{"type":"paragraph","text":"Login and password reset endpoints should be protected against abuse. Rate limiting, account lockout strategies, strong password policies, and monitoring can help reduce brute-force attacks and suspicious activity."},{"type":"paragraph","text":"Authentication should be designed as part of the application's architecture rather than added as an afterthought. Password hashing, short-lived access tokens, secure token handling, authorization checks, resource ownership validation, and rate limiting all contribute to a safer Node.js backend."}]},"saveScroll":false}]
7:["$","$L3","nextjs-performance-optimization-for-production-applications",{"post":{"slug":"nextjs-performance-optimization-for-production-applications","title":"Practical Next.js Performance Optimization for Production Applications","excerpt":"Practical techniques for improving Next.js applications using server rendering, image optimization, caching, dynamic rendering, and smaller client bundles.","date":"2026-02-28","readTime":"9 min read","tags":["Next.js","React","Performance","Frontend"],"coverImage":"/blogs/next-performance/1.webp","content":[{"type":"paragraph","text":"A modern frontend can have an impressive design and still feel slow. Large JavaScript bundles, unnecessary client components, unoptimized images, and repeated API requests can quickly affect the user experience. With Next.js, many performance improvements come from making better architectural decisions rather than simply adding more optimization libraries."},{"type":"heading","text":"Use Server Components by default"},{"type":"paragraph","text":"In the Next.js App Router, Server Components are a useful default for pages that do not require browser APIs, local state, or event handlers. Keeping components on the server can reduce the amount of JavaScript that needs to be sent to the browser."},{"type":"code","language":"javascript","filename":"page.jsx","code":"import ProductList from \"@/components/ProductList\";\n\nexport default async function ProductsPage() {\n  const response = await fetch(\n    `${process.env.API_URL}/products`,\n    { cache: \"no-store\" }\n  );\n\n  const products = await response.json();\n\n  return <ProductList products={products} />;\n}"},{"type":"paragraph","text":"The important point is not that Server Components should always be used. Interactive components still need to run on the client. The goal is to keep the client boundary as small as practical."},{"type":"heading","text":"Be intentional with client components"},{"type":"paragraph","text":"Adding \"use client\" to a component changes where that component and its dependencies are executed. I try to keep interactive functionality isolated instead of marking an entire page as a client component just because one small section needs state."},{"type":"code","language":"javascript","filename":"SearchBox.jsx","code":"\"use client\";\n\nimport { useState } from \"react\";\n\nexport default function SearchBox() {\n  const [query, setQuery] = useState(\"\");\n\n  return (\n    <input\n      value={query}\n      onChange={(event) => setQuery(event.target.value)}\n      placeholder=\"Search products...\"\n    />\n  );\n}"},{"type":"heading","text":"Optimize images correctly"},{"type":"paragraph","text":"Images are often responsible for a significant part of a page's transferred data. Using Next.js Image with appropriate dimensions, responsive sizes, and modern formats can improve loading performance while maintaining visual quality."},{"type":"code","language":"javascript","filename":"HeroImage.jsx","code":"import Image from \"next/image\";\n\nexport default function HeroImage() {\n  return (\n    <Image\n      src=\"/images/hero.webp\"\n      alt=\"Developer working on a web application\"\n      width={1600}\n      height={900}\n      priority\n      sizes=\"(max-width: 768px) 100vw, 80vw\"\n    />\n  );\n}"},{"type":"heading","text":"Avoid unnecessary API requests"},{"type":"paragraph","text":"One common performance issue is accidentally requesting the same data multiple times. This can happen when server rendering, client-side effects, and state management libraries all fetch the same resource independently."},{"type":"paragraph","text":"Before adding another API request, I check whether the data can be fetched on the server and passed to the client component as props. When client-side fetching is required, a dedicated data-fetching strategy can help manage caching, loading states, and request deduplication."},{"type":"heading","text":"Use dynamic rendering only when you need it"},{"type":"paragraph","text":"Not every page needs to be dynamically rendered on every request. Pages containing frequently changing or user-specific information may need dynamic rendering, while static or cacheable pages can benefit from pre-rendering and caching."},{"type":"code","language":"javascript","filename":"page.jsx","code":"export const dynamic = \"force-dynamic\";\n\nexport default async function DashboardPage() {\n  const data = await getDashboardData();\n\n  return <Dashboard data={data} />;\n}"},{"type":"paragraph","text":"The best optimization is usually the one you do not need to maintain. Before introducing complex caching or performance tooling, I first look at rendering boundaries, network requests, bundle size, and image loading. These fundamentals usually provide the biggest improvements."}]},"saveScroll":false}]
8:["$","script","script-0",{"src":"/_next/static/chunks/2snm_u37n2zlf.js","async":true}]
9:["$","$Lb",null,{"children":["$","$c",null,{"name":"Next.MetadataOutlet","children":"$@d"}]}]
d:null
